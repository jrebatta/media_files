{
  "version": 2,
  "buildCommand": "npm install && npm start",
  "public": "public",
  "routes": [
    { "src": "/uploads/(.*)", "dest": "/uploads/$1" },
    { "src": "/(.*)", "dest": "server.js" }
  ]
}
